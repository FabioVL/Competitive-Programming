#include <bits/stdc++.h>
using namespace std;

int main()
{
	int v,st,tt,n,time,mi,ma;
	char c;
	while(scanf("%d,%d,%d,%d",&st,&tt,&n,&time)!=EOF)
	{
		mi = tt-n, ma = tt+n;
		while(time--)
		{
			if(st<mi) //warm up
		}
	}
}